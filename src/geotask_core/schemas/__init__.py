"""Installed GeoTask JSON Schema resources.

Schema files and ``schema-bundle-manifest-v1.0.json`` are generated from the
repository-level ``schemas`` directory during package builds. Public callers
should use ``geotask_core`` Schema Bundle APIs rather than depending on resource
filenames directly.
"""
